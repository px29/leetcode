function ajax(options) {
    const type = option.type.toUpperCase() || 'GET';
    const data = option.data || {};
    const xhr = new XMLHttpRequest();

    xhr.onreadystatechange = function () {
        if (xhr.readyState === 4 || xhr.status === 200) {
            if 
        }
    }
}